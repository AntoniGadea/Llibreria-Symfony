{% extends 'base.html.twig' %}
{% block title %}Usuari{% endblock %}
{% block body %}
<h1>Edita el usuari</h1>
   {{ form_start(formulari) }}
    {{ form_widget(formulari) }}
    {{ form_end(formulari) }}
{% endblock %}
